{% extends 'base.twig' %}
{% block title %} Арбитражные управляющие {% endblock %}
{% block content %}
    <h2 style="text-align: center">Арбитражные управляющие</h2>
    <div class="container text-center mt-4">
        <div class="row justify-content-md-center">
            <table class="table">
                <thead>
                <tr>
                    <th scope="col">ИНН</th>
                    <th scope="col">Имя</th>
                </tr>
                </thead>
                <tbody class="tableBody">
                {% for d in data %}
                    <tr>
                        <th scope="row">
                            <a href="http://parserui.loc/arbitrs/arbitr/{{ d.inn }}" target="_blank">{{ d.inn }}
                            </a></th>
                        <td>{{ d.fio }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}